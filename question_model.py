class Question:

    def __init__(self, q_text,q_answer):
        self.text = q_text
        self.answer = q_answer
# here we have created class we have given two attributes and the function is passed .
##new_q = Question("sddsd","false")
##print (new_q.text)
# here we are fetching the values of question .